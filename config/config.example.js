// Example config file. Copy to `config/config.js` and fill values locally.
// This file is safe to commit as it contains no secrets.

const ADMIN_PASSWORD = '';

module.exports = {
  ADMIN_PASSWORD
};

/* Usage:
   - Copy this file to `config/config.js`
   - Set `ADMIN_PASSWORD` to a secure value or set the `ADMIN_PASSWORD` env var
*/
